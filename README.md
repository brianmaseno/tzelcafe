# TZEL CAFÉ — Monorepo

This repo contains:

- `frontend/`: Vue 3 + Vite + Tailwind storefront
- `backend/`: Laravel (Blade) API + Admin Dashboard

## Frontend

```bash
cd frontend
npm install
npm run dev
```

## Backend

```bash
cd backend
composer install
php artisan serve
```

